package org.example.products;

public interface Shirt {

    // Método para verificar si la camisa tiene mangas largas.
    boolean hasLongSleeves();

    // Método para verificar si la camisa tiene botones.
    boolean hasButtons();
}
